# 当前状态

> 本文件是项目唯一的动态状态来源。每轮完成、切换设备或切换 Codex/OpenClaw 前更新本文件；历史快照保留在根目录和 `docs/` 中，不覆盖本文件。

**更新时间**：2026-08-04  
**当前阶段**：P3 / B-M1 智能骨架  
**总体状态**：代码与单测交付，等待 Windows + SolidWorks 真机目视验收

## 当前目标

以 `LB26拉臂装置/LB26.00000拉臂总成.DWG` 为唯一验收基准，验证 B-M1 生成图纸的真实 SolidWorks 视图、布局和逐步快照。人工验收通过后，方可进入 B-M2。

## 已完成

- P0 审查模块完成并保持封存。
- P1 归档与基线完成；DXF 生成路线已废止。
- P2 三项 Spike 完成并验证：尺寸筛选、真图快照、企业模板。
- B-M1 代码与单元测试已交付：类型识别、视图策略、第一角布局、比例规则、标题栏属性键及布局缺陷修复。
- Codex 项目协作基线已初始化：`AGENTS.md`、环境模板、测试依赖分层、交接规范。
- 远端与本地改动已合并：fast-forward `aadb7ac`→`5a162c9`，今日工作区改动零冲突并入；非生成链测试 223 passed 基线保持。

## 待办与门禁

1. **Windows 真机验收（最高优先级）**：在 SolidWorks 2025 中对比 LB26 验收图纸，检查 Step3 的视图插入、比例、轮廓获取、轴测视图名以及逐步快照。
2. 真机通过后，提交/推送 B-M1 验收结论并确认进入 B-M2。
3. B-M2：Step5 原生 BOM + 球标；Step6 `InsertNote` 技术要求。
4. 为其余 4 类零件补充案例图校准。

## 已知限制与风险

- 真实 SW COM API 尚待 Windows 真机验证；不可将 mock/单元测试当作真机验收。
- Mac 当前不能完整收集生成链测试：`app/generators/sw_com.py` 和 `app/parsers/sw_parser.py` 顶层导入 `pythoncom`/`win32com`。跨平台导入兼容性需另立任务改造。
- 测试基线（合并后 Mac 实测）：`pytest tests/ --continue-on-collection-errors` → 223 passed / 16 skipped / 11 errors（生成链 pythoncom）/ 12 failed（pipeline async，缺 `pytest-asyncio`；补装 1.4.0 后可收集 15 项，但 Mac 资源限制下 exit 137 未完整复验）。完整生成链验证留 Windows。
- ODA File Converter 为可选外部依赖，须以 Windows 本机的 `ODA_PATH` 配置为准。
- Git 远端推送曾因网络连接超时受阻；每次切换前确认分支是否已推送。

## 本轮验收口径

- 日常代码：运行受影响测试，并如实报告未运行项。
- SW 相关代码：Windows + SolidWorks 真机操作、快照/图纸目视对比和结论必须记录到本文件与交接记录。
- 任何生成结果均不得覆盖 `LB26拉臂装置/` 内的样本资产。
