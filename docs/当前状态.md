# 当前状态

> 本文件是项目唯一的动态状态来源。每轮完成、切换设备或切换 Codex/OpenClaw 前更新本文件；历史快照保留在 `docs/进度状态-YYYY-MM-DD.md`，不覆盖本文件。

**更新时间**：2026-08-04（晚）  
**当前阶段**：P3 / B-M1 智能骨架（**未真正完成**）  
**总体状态**：后端代码+单测已交付；**阶段产物页已交付待真机验收**；后端布局逃生口修复已派出（进行中）；真机验收未过

## 当前卡点（最高优先级）

~~B-M1 前端未完成~~（08-04 晚已交付）。当前两件事并行：
1. **老板真机验收**：起服务 → 生成页上传 LB26 模型 → 阶段页「骨架」卡目视对比 LB26.DWG（前端验收 + 图纸质量验收一并做）
2. **后端布局修复**（08-04 晚派出）：重排失败逃生口 + 轴测/俯视争位 + 视图名硬编码

## 已完成

- P0 审查模块完成并保持封存。
- P1 归档与基线完成；DXF 生成路线已废止。
- P2 三项 Spike 完成并验证：尺寸筛选、真图快照、企业模板。
- B-M1 后端代码与单元测试已交付：Step3 类型识别、视图策略、第一角布局、比例规则；Step7 标题栏属性键及布局缺陷修复。
- B-M1 前端步骤回放基础已实现：`views.js` 的 `renderTimeline`/`renderStepDetail`、真图快照（`snapshot_url`/`snapshotUrl`/`artifactUrl`）。
- **B-M1 前端阶段产物页已交付（08-04 晚）**：五阶段分组（导入/骨架/表格/标注/审查）+ 骨架卡快照与全格式下载 + 表格/标注空态占位 + 表单删 drawing_type/target_format + 阶段页/时间线切换；变更 3 文件（views.js/main.js/generate.html），node --check 通过，浏览器渲染待真机验收。
- **主代理代码审查（08-04 晚，老板指派）**：视图重叠处理链（估算→实测重选比例→迭代重定位→只告警）现状在案；定位逃生口根因 = 5b 实测重排失败 positions={} 导致跳过重定位。
- Codex 项目协作基线已初始化：`AGENTS.md`、环境模板、测试依赖分层、交接规范。
- 2026-08-04 远端与本地改动已合并（fast-forward `aadb7ac`→`5a162c9`），零冲突；非生成链测试 223 passed 基线保持。
- 2026-08-04 今日对 Step3/Step7 代码进行审查（审查报告未留存，结论未归档）。

## 未完成 / 卡点

1. **B-M1 前端未完成**：步骤回放基础有，但要能支撑真机验收的程度还不够；08-03 已重构前端方案（`07-前端五层与步骤回放.md` 8.5 阶段产物页 + `assets/10-B-M2-表格与注释.md`），阶段产物页尚未实现（`views.js` 无 `renderStage` 分组逻辑）。
2. **B-M1 真机验收未过**：计划通过前端步骤回放目视对比 LB26.DWG，卡在前端实现。

## 待办与门禁

1. **B-M1 真机验收（老板）**：前端阶段页 + 图纸质量一并验；后端布局修复交付后需重新生成再验图纸。
2. ~~完成 B-M1 前端~~（已交付）。
3. 通过前端完成 B-M1 真机验收（目视对比 LB26.DWG），验收通过方可进入 B-M2。
4. B-M2：Step5 原生 BOM + 球标；Step6 `InsertNote` 技术要求；**前置：补 api.getStepOutput(taskId,5/6) 后端路由与前端封装**（08-04 dev 报告的契约缺口）。
5. 为其余 4 类零件补充案例图校准。

## 已知限制与风险

- 真实 SW COM API 尚待 Windows 真机验证；不可将 mock/单元测试当作真机验收。
- Mac 当前不能完整收集生成链测试：`app/generators/sw_com.py` 和 `app/parsers/sw_parser.py` 顶层导入 `pythoncom`/`win32com`。跨平台导入兼容性需另立任务改造。
- 测试基线（合并后 Mac 实测）：`pytest tests/ --continue-on-collection-errors` → 223 passed / 16 skipped / 11 errors（生成链 pythoncom）/ 12 failed（pipeline async，缺 pytest-asyncio；补装 1.4.0 后 Mac 资源限制 exit 137 未完整复验）。完整生成链验证留 Windows。
- `app/generators/view_strategy.py:50-56` 视图名硬编码（08-03 审查阻断级）~~仍未修复~~ → **已并入 08-04 晚布局修复任务**（抽为可配置，默认行为不变）。
- ODA File Converter 为可选外部依赖，须以 Windows 本机的 `ODA_PATH` 配置为准。
- Git 远端推送曾因网络连接超时受阻；每次切换前确认分支是否已推送。

## 本轮验收口径

- 日常代码：运行受影响测试，并如实报告未运行项。
- SW 相关代码：Windows + SolidWorks 真机操作、快照/图纸目视对比和结论必须记录到本文件与交接记录。
- 任何生成结果均不得覆盖 `LB26拉臂装置/` 内的样本资产。
