# 当前状态

> 本文件是项目唯一的动态状态来源。每轮完成、切换设备或切换 Codex/OpenClaw 前更新本文件；历史快照保留在 `docs/进度状态-YYYY-MM-DD.md`，不覆盖本文件。

**更新时间**：2026-08-05 19:20
**当前阶段**：P3 / B-M1 智能骨架（方案已扩展为**完整工程图生成**）
**总体状态**：2026-08-05 下午老板确认完整方案方向；之前 dev 实现因重复造轮子被回退；步骤1 数据模型扩展已完成（主代理直干），代码从零重做进行中

---

## 今天已完成（2026-08-05）

1. **老板确认完整方案方向**：
   - 视图类型扩展：标准视图 + 轴测 + 局部放大（DETAIL）+ 剖视（SECTION）+ 辅助视图（AUXILIARY）
   - 多比例并存：主比例自动算，辅助比例用户指定或默认 2x（如主视 1:50 → 局部 1:25）
   - 布局引擎：从"固定四位置"改为"主视优先+辅助填充"
   - 用户辅助指定：类型纠偏 / 视图组合勾选 / 位置拖拽（L3 后置）
   - 检查点重跑：任意步骤可覆盖参数后局部重跑
   - 执行顺序：`1→2→3→7→5→6→4→8`
   - BOM 仅预留位置，当前不做生成

2. **SW API 参数确认**：
   - `Create1stAngleViews2`（第一角投影，GB标准）
   - `CreateDrawViewFromModelView3`（轴测图）
   - `CreateDetailViewAt3`（局部放大，参数已确认）
   - `CreateSectionViewAt4`（剖视，参数已确认）

3. **工作模式切换（老板拍板）**：主代理全栈直干为默认模式；子代理休眠备用，老板显式点名启用。规程已写入主代理 SOUL.md。

4. **设计文档更新**：
   - `docs/02-方案B技术设计.md` 追加 §3.x 视图策略与数据模型、§5.x 检查点重跑与用户辅助

5. **步骤1 数据模型扩展完成（主代理直干，145 项测试全绿）**：
   - `app/generators/view_strategy.py`：ViewName 补 back/bottom + SW 预定义名映射；ViewConfig 按设计文档对齐——`region` 改为 `{"center":(x,y,z), "radius":float}`（模型空间绝对坐标 mm）、`cut_line` 改为点列表 polyline、`position_mode` 收敛为 auto/hint/absolute；ViewStrategy 新增 `layout_mode`（auto/manual）；`apply_overrides` 新增 `layout_mode` 与 `positions_override`（按 id/name 匹配→强制 absolute 定位，未匹配/非法坐标告警跳过）；新增 `resolve_scale_denominator`（多比例并存：数值=相对主比例放大倍数，"1:N"=绝对分母）；`to_layout_input` 按各视图独立比例缩放并透传 position_params/cut_line
   - `app/generators/type_recognition.py`：`DEFAULT_VIEW_SUGGESTIONS` 结构化为 view_type/name/display_name/position_hint/scale 五字段
   - `app/models/generation.py`：`TaskConfig` 新增 5 个覆盖字段（part_type_override/views_override/layout_mode/positions_override/projection_type_override）+ 2 个校验器（非法类型/非法坐标直接 ValueError）
   - 测试：test_view_strategy_bm1p.py 全面重写对齐新语义 + 新增 layout_mode/positions_override/region·cut_line 校验用例；test_type_recognition.py 新增 suggested_views 结构化用例；test_models.py 新增覆盖字段用例

---

## 当前卡点

1. **B-M1 代码从零重做进行中**：步骤1（数据模型）✅；步骤2（布局引擎重写 layout.py）未开始
2. **B-M1 真机验收仍未进行**：前端阶段产物页已交付，但后端骨架未跑通，无法验收

---

## 待办（按优先级）

| 优先级 | 事项 | 说明 | 状态 |
|--------|------|------|------|
| P0 | ~~数据模型扩展~~ | view_strategy / type_recognition / generation | ✅ 2026-08-05 |
| P0 | **布局引擎重写** | 新建 `layout.py`：主视优先 + 辅助视图填充剩余空间 + 重叠检测 | 待开工 |
| P0 | **SW API 层新增** | `sw_com.py`：Create1stAngleViews2 / CreateDrawViewFromModelView3 / CreateDetailViewAt3 / CreateSectionViewAt4 | 待开工 |
| P1 | **前端参数面板** | 骨架卡展开：类型纠偏、视图勾选、比例设置、局部放大圈选 | 待开工 |
| P1 | **检查点重跑接口** | `POST /api/generate/{task_id}/rerun`，from_step + overrides | 待开工 |
| P1 | **B-M1 真机验收** | 起服务 → 上传 LB26 模型 → 阶段页目视对比 | 待开工 |
| P2 | **BOM 预留位置** | Step5 表格卡显示"预留"占位，不实现生成 | 待开工 |

---

## 已知限制与风险

- **真实 SW COM API 尚待 Windows 真机验证**；不可将 mock/单元测试当作真机验收
- **Mac 不能完整收集生成链测试**：`pythoncom`/`win32com` 顶层导入报错，完整验证留 Windows
- **子代理简报必须精确**：预算 ≤50 行 / ≤30k tokens / 测试 ≤5 个；超预算主动汇报
- **SW 进程纪律**：严禁强杀；弹窗卡死一律喊人

---

## 本轮验收口径

- 代码变更：严格在文件清单范围内，只跑受影响测试文件
- SW 相关：Windows + SolidWorks 真机操作、快照目视对比结论记录到本文件
- 任何生成结果不得覆盖 `LB26拉臂装置/` 内样本资产
