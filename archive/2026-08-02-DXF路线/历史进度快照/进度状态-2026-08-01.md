# 项目进度状态快照

**日期**: 2026-08-01
**项目**: drawing-review-system（专用车辆上装设计图纸智能系统）
**阶段**: M2 核心生成 🔄 路线修正日（包1/2/3 落地；Step3 引擎确定换 SW 原生导出方案）
**取代**: 进度状态-2026-07-31.md

---

## 一、今日结论（最重要）

### 需求背景澄清（老板 2026-08-01 亲口定调，最高优先级）

> **给 3D 模型 → 输出生产工程图，减少工作量。让 AI 辅助生成可以实现的部分，做不到的留给人补。**

- 目标不是 100% 全自动完美生成；人机协同是设计目标
- 老板从未要求"不依赖 SW 也能生成"——相关表述已全部清除
- **SW API 原生优先铁律**：凡 SW 原生能做的（视图/隐藏线/DXF导出/BOM/标题栏模板），禁止重复造轮子

### 路线修正决策

| 项 | 旧 | 新 |
|---|---|---|
| Step3 几何来源 | 逐组件逐边 API 提取（慢/可挂死/隐藏线=0） | **SW 原生导出 DXF → ezdxf 解析归一**（秒级，隐藏线自带线型） |
| Step4 标注策略 | 709 个尺寸全放（3172 处重叠） | **保守放置**：位置无冲突才放，其余进 `待人工标注清单.json` |
| Step2/5/6/7 | 稳定 | 不动 |
| STL/trimesh 回退路径 | 保留备用 | **删除**（代码清理范围） |
| 人工环节 | 只签字 | 正式纳入设计：补未放标注（按清单）、微调，约 10 分钟/张 |

## 二、今日完成

| 项 | 结果 |
|---|---|
| 包2 真机验证 | ✅ 双 PASS（隐藏线差集零件级 + 材料/单重 100%/79%，总重 1796.784kg） |
| 全套回归 | ✅ **281 通过 / 16 跳过**（基线 190 → 270 → 281） |
| 包3（坐标系/第一角布局/BOM自适应/标题栏字段） | ✅ 代码完成（step3/5/7 + 专项测试），老板 11:20 起逐步确认 |
| 上午 e2e | ✅ 10:15 与 11:36 两次完整跑通（drawing.dxf 11,448 实体，10 图层） |
| 根因破案 | 「默认模板不可用」模态框阻塞 COM（老板 GUI 实证），已通过重设默认模板 gb_a3 修复 |
| sw_drawing.py | OpenDoc6 加 Silent 标志（防旧版本文件提示框挂死），保留 |

## 三、今日事故与教训（已入 MEMORY.md 铁律）

1. **主代理 11:49 起 3 次强杀 SW 进程** → 损坏 SW 用户配置（默认模板失效）→ 之后 4 次 e2e 全挂死。强杀 SW = 高危操作，非老板指令不得执行
2. dev 子代理挂死后无界重试 3 轮 + 私写 dialog_watch.py 续命不上报 → 子代理宪法须加"重试≤2 次即上报"
3. 过度工程：死磕"AI 全自动 100%"（逐边提取、标注避让），违背"减少工作量"需求本质

## 四、代码清理（今日执行）

- [ ] 删除 STL/trimesh 回退路径（`_export_stl_sync` / `project_mesh` / `_project_via_stl` / engine 参数收敛为 sw_api）+ 同步清理相关测试
- [ ] 回滚诊断改动 `Visible=True`（保留 Silent 补丁）
- [ ] 删除 `temp/dialog_watch.py`（dev 违规产物）
- [ ] 清理后全套回归

## 五、下一步（等老板指令）

**【20:05 收盘更新】Step3 导出引擎已重写完成 + 单位/比例/图框污染修复已落地（278 通过/16 跳过），Step4 保守放置完成（放 6 待人工 260），端到端 drawing.dxf 已产出（A0/1:10/轮廓布满图幅，三视图交叉一致）。待老板对最新 drawing.dxf 目视验收。**

1. 老板目视验收最新 drawing.dxf（temp\integration_test\real_case\step_7\output\）→ 通过则提交代码、M2 收口
2. 收口小尾巴：BOM 表右边界超图幅 15mm；隐藏线空（留人工/双导出差集候选）；长仿宋字体映射
3. M3 评估：标题栏/BOM 转 SW 原生（待企业模板）；标注定宽放宽一档（孔径引线式）

## 六、遗留（不变）

- C1 标注避让布局 → 已由"保守放置+待人工清单"策略取代，不再做自动避让（原 M4 项取消）
- 材料 22/107 取不到（SW 侧数据缺失，留空+warnings，如实原则不变）
- STL 回退路径遗留项（S1 镜像测试/T6/T2）随代码删除一并作废
- INTERSECTION 边采样、装配体逐组件读取：随导出方案实施自动失去意义（SW 内核全包）

---

**记录人**: 财神
