# 归档说明 · 2026-08-02 DXF 路线废止

## 为什么归档

2026-08-02 老板目视验收 M2 产物 drawing.dxf，判定 **DXF 线稿载体不满足交付形态**（图纸页为空、视图无关联、假标注、假 BOM）。对照基准 `LB26拉臂装置/LB26.00000拉臂总成.SLDDRW`，DXF 路线整体废止，项目切换至 **方案B：SW 原生 SLDDRW 路线**（设计见 `docs/plans/08-方案B-SW原生SLDDRW改造设计.md`，总计划见 `docs/plans/09-项目总计划-0to1.md`）。

## 教训（防再犯）

1. **方案审查的基线必须是用户可验收的真实样例，不是内部设计文档**——用设计文档验证方案等于自证
2. **验收第一项永远是"目视对比案例图纸"**，每里程碑强制执行
3. 载体选择要由"用户验收什么"决定，不由"工程上好不好做"决定

## 归档内容

| 归档物 | 原位置 | 废止原因 |
|--------|--------|----------|
| 历史进度快照 ×5 | 项目根目录 | 2026-07-28 ~ 08-01 DXF 路线时期的进度记录，封存留档 |

## 代码级废止（声明式归档，物理删除随方案B重写执行）

以下代码已判定废弃，但为保证基线随时可跑（278 通过/16 跳过），**暂不物理移除**，待方案B 对应执行器重写落地时随替换删除：

| 废弃件 | 废止原因 | 删除时机 |
|--------|----------|----------|
| step3_view_project.py 渲染层（ezdxf 拆线归一） | DXF 载体专属 | B-M1 重写 Step3 时 |
| step4_dimension.py 渲染层（三线假标注） | 非 SW 标注对象 | B-M3 重写 Step4 时 |
| step5_bom_generate.py 渲染层（自绘表格线） | 非原生 BOM | B-M2 重写 Step5 时 |
| step7_dxf_build.py（ezdxf 拼装 DXF） | 模型空间假图纸 | B-M1 重写 Step7 时 |
| view_extractor.py | DXF 载体专属 | B-M1 时 |
| placeholders.py | M1 空骨架 | B-M4 全部执行器就位后 |

以上文件中的**可移植逻辑**（布局引擎/保守策略/冲突检测/BOM 聚合）在重写时提取复用，详见 `docs/plans/09-项目总计划-0to1.md` 复用清单。

## 未归档但标记废止的文档

- `docs/plans/01-AI生成DWG工程图方案设计.md`：DXF 载体部分废止（文档头部已加废止注记），AI 决策思路等仍有参考价值
- `docs/reviews/M2全面审查-上半区/下半区-2026-08-02.md`：方案合理性结论已勘误（见报告头注）；代码合理性发现（S1-S4 等）在方案B重写时仍需参考

## 复用资产索引（未归档，继续服役）

骨架全活：pipeline 状态机、sw_com 单线程执行器、sw_drawing COM 封装、Step1/Step2/Step6、core/models/routers/services、审查模块、前端五层架构。详见 `docs/plans/09-项目总计划-0to1.md` 复用清单。

## 历史基线

- 本地 git：`ff42bc6` = M2 DXF 路线最终状态（281 通过/16 跳过），永久可回溯
