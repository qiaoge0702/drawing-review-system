# CONTEXT.md - 项目上下文舱单

> 项目切换标准：主代理切入本项目时，读本文件 + 最新进度快照即可开工。
> 本文件只写稳定信息，动态进度见 `进度状态-YYYY-MM-DD.md`（读最新一份）。

## 1. 项目定位

专用车辆上装设计图纸智能系统 —— 基于 AI 大模型的图纸全生命周期系统：`3D模型(SolidWorks) → AI生成SLDDRW/DWG → AI审查DWG → 人工审核签字`。

- 审查模块：✅ 已完成（P0，封存不动）
- 生成模块：🔄 **方案B（SW 原生 SLDDRW）重建期** —— 2026-08-02 DXF 路线废止后从零起新账
- 目标量化：纯手工 2.5-4h/张 → 15-30min/张（含人工约 10min 把关），节省 80-90%
- 人机协同是设计目标：AI 做判断（尺寸筛选/逐步质检守门），SW 画一切，人终审

## 2. 技术栈快照

| 层级 | 技术 | 版本锁定 |
|------|------|----------|
| 出图引擎 | **SW 原生 API（视图/标注/BOM/标题栏/SaveAs DWG·PDF）** | SolidWorks 2025 正式版 |
| 3D解析 | pywin32 (SW COM)，一律走 sw_com 单线程执行器 | pywin32 312 |
| AI模型 | Kimi K3 / GPT-4o（多模态：尺寸筛选 + 每步质检） | — |
| 后端 | FastAPI + Pydantic 2.0 + asyncio | FastAPI 0.140.7 / Python 3.12.10 |
| 前端 | Vanilla JS + ES Module（**Vue3 重写计划已取消**） | — |
| 企业模板 | .drwdot（从 LB26.SLDDRW 反存，P2 S-3 产出） | — |

## 3. 目录拓扑

```
drawing-review-system/
├── app/
│   ├── main.py            # FastAPI 入口
│   ├── core/  models/  routers/  services/   # ✅ 骨架复用
│   ├── parsers/  ai/  rules/  renderers/     # ✅ 审查模块（P0 成品，不动）
│   ├── generators/        # pipeline.py + sw_com.py + sw_drawing.py（✅ 复用）
│   │   └── steps/         # step1/2/6 ✅ 复用；step3/4/5/7 🔄 方案B 重写
│   └── templates/ + static/generate/js/      # ✅ 复用，扩展步骤回放/工作台
├── tests/                 # 未改动模块基线保留；step3/4/5/7 测试随重写更新
├── docs/plans/            # 08-方案B设计 + 09-项目总计划（现行）；00-07 历史参考
├── docs/reviews/          # 审查报告（M2 两份已勘误，代码发现仍有效）
├── archive/2026-08-02-DXF路线/   # DXF 路线废弃件 + 历史进度快照 + 归档说明
├── LB26拉臂装置/          # 案例数据 + 验收基准（LB26.00000拉臂总成.DWG 完成图纸；SLDDRW 为空白模板仅供参考）
└── 进度状态-YYYY-MM-DD.md # 进度快照（新账自 2026-08-02 起，读最新）
```

## 4. 当前里程碑 + 下一步

- **当前**: P1 资产归档与基线固化（2026-08-02）
- **下一步**: P2（B-M0）三 Spike —— S-1 尺寸筛选 / S-2 真图快照 / S-3 企业模板
- **总计划**: `docs/plans/09-项目总计划-0to1.md`（P0→P7，前后端共同推进）
- **验收基准（唯一）**: LB26.00000拉臂总成.DWG（完成图纸，2026-08-02 老板确认）；每里程碑第一项验收 = 目视对比

## 5. 禁区与约定

- **SW API 原生优先**：凡 SW 原生能做的（视图/隐藏线/标注/BOM/标题栏/DWG导出）禁止自研
- **SW COM 纪律**：一切 COM 调用经 `sw_com.py` 单线程执行器；**严禁强杀 SW 进程**；OpenDoc 必带 Silent
- **API 契约已冻结**：`/api/generate` 系列请求/响应模型只加不改
- **检查点纪律**：仅成功步骤写检查点；失败/损坏一律视为无效重跑
- **测试红线**：未改动模块测试基线不得打破；新增能力必须带测试
- **如实原则**：取不到的数据留空+warnings，禁止编造
- **方案审查基线 = 真实样例**（LB26 案例图），不是内部设计文档
- 全局铁律见主代理 SOUL.md / MEMORY.md（严禁蔓延 ≤5文件/≤200行、完全遵循设计、闭环留痕）
