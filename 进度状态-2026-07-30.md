# 项目进度状态快照

**日期**: 2026-07-30
**项目**: drawing-review-system（专用车辆上装设计图纸智能系统）
**阶段**: M1 框架搭建 ✅ 收口完成
**取代**: 进度状态-2026-07-29.md

---

## 一、里程碑进度

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| M0 基线巩固 | ✅ 完成 | — |
| **M1 框架搭建** | ✅ **完成（2026-07-30）** | 两大门禁均达成 |
| M2 核心生成 | ⏳ 就绪 | Step3-7 真实执行器待开发（LB26 焊接件为目标） |

## 二、M1 门禁验收

| 门禁 | 验证方式 | 结果 |
|------|----------|------|
| 流水线可空跑 | `scripts/dry_run_pipeline.py`：8步占位执行+检查点复用+单步重跑 | ✅ |
| API 契约冻结 | `/api/generate` 系列路由注册 + OpenAPI 自动生成 + Pydantic 严格校验（422 拦截非法参数） | ✅ |

## 三、今日完成项

| 任务 | 成果 |
|------|------|
| 代码审查 | reviewer 子代理输出 `docs/reviews/M1-骨架代码审查-2026-07-30.md`，6 阻断 + 9 建议 + 8 提示 |
| 阻断级修复 | B1 失败检查点误用 / B2 COM 阻塞事件循环 / B3 COM 资源泄漏 / B4 假截图路径 / B5 重试计数错误 / B6 检查点损坏崩溃 |
| Step3-8 占位 executor | `app/generators/steps/placeholders.py`（M2 逐步替换为真实执行器） |
| SW COM 线程层 | `app/generators/sw_com.py`：单线程 executor + CoInitialize，COM 亲和 + 串行化 |
| 生成任务服务 | `app/services/generation_service.py`：任务注册表、串行队列、进度回调、磁盘恢复 |
| API 路由 | `app/routers/generate.py`：create/list/get/rerun/artifacts 下载（防路径穿越） |
| WS 进度推送 | 流水线 `set_progress_callback` 钩子 → step_start/step/finished 三类事件 |
| 前端 `/generate` | Vanilla JS + 严格 ES Module 分层（api/store/ws/views/main），为 M3 Vue3 迁移锁死低成本 |
| 测试补齐 | 失败检查点/重试计数累加/非重试步骤/损坏检查点 4 项回归测试 |

## 四、测试基线

| 指标 | 数值 | 较昨日 |
|------|------|--------|
| 通过 | **153** | +4 |
| 跳过 | 16 | — |

## 五、附带发现（已修）

- `SWException` 不接受 task_id/step 参数 → 已扩展签名
- 流水线未把 `source_file` 注入步骤参数 → 已修
- 实测环境 SolidWorks 33.3.0 可正常 COM 连接

## 六、遗留（建议级，随 M2 清理）

- S3 rerun 参数覆盖用 `model_copy(update=)` 重新校验
- S4 同 task_id 并发互斥锁
- S8 `TaskResult.output_file` 赋值、rerun 谱系、`StepResult.logs` 写入
- T2 `datetime.utcnow()` 弃用替换（全局 330 条 warning 来源）

## 七、下一步（M2 核心生成，第 4-6 周）

1. Step3 视图投影真实执行器
2. Step7 DXF 构建（ezdxf）
3. 目标：LB26 焊接件生成可打开、结构正确
4. **风险**：SW 试用 30 天到期前完成核心（见计划总览风险表）

## 八、子代理体系落地（同日）

- 四子代理就位：dev / tester / reviewer / doc-sync，各自独立工作区宪法（SOUL/AGENTS/TOOLS/USER）
- 主代理 `subagents.allowAgents` 白名单已配置；子代理工具 = coding profile + deny 减法
- 协作纪律：单向流水线，禁止互调，主代理中转
- 项目级 `CONTEXT.md` 已建立（五段式舱单），支持一句话切换项目上下文
- 复审：M1 收口代码第二轮 reviewer 审查进行中（待网关重启后执行）

---

**记录人**: 财神
