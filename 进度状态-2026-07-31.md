# 项目进度状态快照

**日期**: 2026-07-31
**项目**: drawing-review-system（专用车辆上装设计图纸智能系统）
**阶段**: M2 核心生成 🔄 进行中（Step3 ✅ 完成）
**取代**: 进度状态-2026-07-30.md
**备注**: 昨日因网关限流未更新；07-30 下午实际已完成 Step3 全部工作（SW API 重写 + 真机集成），本文档据实收口。

---

## 一、里程碑进度

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| M0 基线巩固 | ✅ 完成 | — |
| M1 框架搭建 | ✅ 完成（2026-07-30） | 两大门禁均达成 |
| **M2 核心生成** | 🔄 **进行中** | Step3 视图投影 ✅ 完成；Step7 DXF 构建待启动 |

### M2 步骤进度（Step 3-7 全部要做，非仅 Step7）

| 步骤 | 状态 | 说明 |
|------|------|------|
| Step3 视图投影 | ✅ 完成（2026-07-30） | 双引擎 + reviewer 可合入 + 建议级修复已落地；待老板人工验收 |
| Step4 尺寸标注 | ✅ 完成（2026-07-31） | 契约全字段；真实案例暴露布局重叠问题 → 收口清单 C1 |
| Step5 BOM 生成 | ✅ 完成（2026-07-31） | 真实案例 LB26.11000 验证通过（43 行聚合 BOM） |
| Step6 技术要求 | 🔄 开发中 | 模板 + 变量填充（焊接件通用模板等），doc 04 第五节契约 |
| Step7 DXF 构建 | ✅ 完成（2026-07-31） | 9 层契约图层 + 真实案例 DXF ezdxf 回读验证通过；布局比例问题 → 收口清单 C2 |

## 二、Step3 完成要点（07-30 下午，昨日未记录部分）

### 1. 技术路线切换：STEP/STL 自写投影 → SW API 工程图视图引擎

- 依据：`docs/SW-API侦察报告-2026-07-30.md`（8 个 probe 脚本实证），老板批准
- 理由：SW 自带 HLR 内核算轮廓/遮挡，我们只做数据搬运；消除 OCC/cascade 依赖风险
- 新增模块：
  - `app/generators/sw_drawing.py` — SW 工程图视图引擎封装（COM 边界层）
  - `app/generators/view_extractor.py` — 实体提取/类型判别/参数化/手动矩阵转 2D
- STL/trimesh 版 **降级为 fallback 保留不删**（无 SW 环境备用路径）
- 引擎选择：`engine = sw_api | stl | auto`（默认 auto，SW 不可用自动回退）
- **契约不变**：views.json 结构与 doc 04 第二节一致，下游零影响

### 2. reviewer 审查（07-30 12:47，STL 版）→ 可合入，建议级修复已落地

| 建议 | 处置 |
|------|------|
| S2 `views=[]` 静默默认值 | ✅ 已修：空列表显式抛 SWException，杜绝"假成功" |
| S3 契约缺 `section_hatch` 键 | ✅ 已修：补 `None` 占位，契约键完整 |
| S1 第一角投影轴向符号约定 + 镜像测试缺口 | ⏳ 遗留：SW API 路径由 SW 内核保证视图正确性，风险已大幅降低；STL 回退路径待补非对称几何镜像测试 |

### 3. 真机集成验证（07-30 14:08，LB26.11202轴套.SLDPRT）

`scripts/integration_step3_swapi.py`，engine=sw_api：

| 视图 | 实体 | 隐藏线 |
|------|------|--------|
| 主视图 | circle × 4 | 0 |
| 俯视图 | circle × 4 | 0 |
| 左视图 | circle × 4 | 0 |

- 轴套为回转体，4 个真圆实体符合预期（SW API 输出真圆/真线，非折线离散）
- **隐藏线未取得**：type 码 0/2 + 显示模式切换均返回空，已按纪律**如实写入 warnings 落盘**（禁止静默降级），攻关方案见侦察报告"遗留未知项"
- `INTERSECTION` 边采样失败 1 处，同样记入 warnings

## 三、测试基线

| 指标 | 数值 | 较 M1 收口 |
|------|------|-----------|
| 通过 | **190** | +37 |
| 跳过 | 16 | —（真实 DXF 样例文件缺失，同前） |
| 全套耗时 | ≈3 分钟 | — |

新增：`test_step3_sw_api.py`（view_extractor 单元 + 契约字段）、`test_step3_view_project_independent.py`（tester 独立验证 11 用例：失败不落盘/退化网格/契约键）

## 四、遗留与下一步

### M2 收口清单（真实案例暴露，老板 2026-07-31 批示后续处理）

- **C1. Step4 标注布局质量**：LB26.11000 真实案例 placement_score=-2.71、overlaps=2628（703 个圆特征直径标注文字固定偏移致大量重叠）。修法：①评分 clamp 到 [0,1]；②重叠检测升级为标注框级；③自动避让布局推 M4（AI 布局优化）。数据正确性不受影响。
- **C2. Step3 布局比例**：真实案例 view_positions/bbox 达 ±50000mm（scale 1:50 未换算），视图落图远超 A3 图幅。Step7 按契约严格执行平移公式、不做二次缩放（docstring 已注明），比例换算/图幅适配归 Step3 布局职责，收口时修。
- **C3. 真实案例 Step6 未跑**：real_case 串跑时 tech_requirements.json 缺失，Step7 降级正确（跳过技术要求层）；收口验证时需补跑。

### 遗留（M2 收口前处理）

1. STL 回退路径：非对称几何镜像测试（S1）、`SaveAs3` 传 Silent 选项（T6）、STEP 注释残留清理（T2）
2. 隐藏线攻关：备用方案 = tessellation 自算（已验证可行）
3. INTERSECTION 边（样条/交线）采样：`GetCurveParams3 + Evaluate` 离散
4. 装配体视图支持（焊合图是装配视图，GetVisibleComponents 逐组件读）
5. M1 遗留建议级项（见 07-30 快照第六节）

### 下一步（按序，Step4-7 全部在 M2 范围内）

1. **老板人工验收 Step3**（见下节）
2. **Step4 尺寸标注** → **Step5 BOM 生成** → **Step6 技术要求** → **Step7 DXF 构建**
3. 收口验证：LB26 焊接件端到端生成，DXF 在 AutoCAD/SW 可打开、结构正确（质量门禁）
4. Step3 装配体视图支持（收口验证需要时提前）

## 二点五、Step3 人工验收方式（老板用）

当前前端 `/generate` 只能跑任务 + 下载产物，**无可视化**（M3 范畴）。现阶段两条路：

| 方式 | 命令 | 看什么 |
|------|------|--------|
| **A. SVG 直出（推荐）** | `python scripts/demo_extract_render.py` | 浏览器打开 `output/demo/views.svg`，目视三视图轮廓/比例/镜像 |
| **B. 数据级** | `python scripts/integration_step3_swapi.py` | 实体统计（轴套三视图应各 4 个真圆）+ views.json 抽查坐标 |

可选增强：补一个 `views.json → SVG` 转换脚本，验收流水线产物时直接可视化（约 10 分钟工作量，待老板确认是否需要）。

## 五、环境事实更新

- SW 版本已从"2024 试用版 30 天 ⚠️"更正为 **SolidWorks 2025 正式版**（CONTEXT.md 已同步），许可无到期风险，STEP 备用路径降级为可选增强

---

**记录人**: 财神
